# 안드로이드 스튜디오 앱 실행/빌드 가이드 (상세)

안드로이드 스튜디오를 켜고, 앱을 실행하거나 APK 파일을 만드는 과정을 처음부터 차근차근 설명해 드립니다.

## 1. 프로젝트 불러오기 (Open)

가장 중요한 단계입니다. 엉뚱한 폴더를 열면 실행 버튼이 나오지 않습니다.

1.  **안드로이드 스튜디오 실행**: Android Studio 아이콘을 더블 클릭해 켭니다.
2.  **Open 클릭**: 시작 화면에서 **[Open]** 버튼을 누르거나, 상단 메뉴 `File > Open`을 누릅니다.
3.  **경로 찾아가기**: 아래 경로를 정확히 찾아가서 **`android` 폴더 아이콘**을 선택하고 [OK]를 누르세요.
    - `c:\Users\smhrd\Desktop\zkapfk\project1\p2\project\frontend\android`
    - _(주의: `frontend` 폴더가 아니라 그 안에 있는 `android` 폴더를 선택해야 합니다! 안드로이드 로봇 아이콘이 있는 폴더입니다.)_
4.  **기다리기**: 프로젝트 창이 열리고 우측 하단에 파란색 로딩 바(Gradle Sync)가 돌아갑니다. 이게 멈출 때까지 **기다려주세요** (몇 분 걸릴 수 있습니다).

---

## 2. 휴대폰 연결하기

1.  **USB 연결**: 개발자 모드(USB 디버깅)가 켜진 안드로이드 폰을 PC에 USB로 연결합니다.
2.  **디바이스 선택**: 안드로이드 스튜디오 상단 중앙에 있는 **디바이스 선택 메뉴**(보통 `No Device` 또는 `Pixel...` 이라고 적힘)를 클릭합니다.
3.  **내 폰 선택**: 연결한 휴대폰 이름(예: `Galaxy S24`, `LG Phone`)을 선택합니다.

---

## 3. 앱 실행하기 (테스트용)

휴대폰에 앱을 바로 설치해서 확인하는 방법입니다.

1.  **재생 버튼 클릭**: 상단 메뉴바의 **초록색 삼각형 재생 버튼(▶ Run)**을 누릅니다.
2.  **확인**: 잠시 후 휴대폰에 앱이 자동으로 켜집니다.
    - _현재 개발 서버(`npm run dev`)와 연결되어 있어, PC 코드를 고치면 폰에서도 바로 바뀝니다._

---

## 4. APK 파일 만들기 (설치 파일 추출)

다른 사람에게 보내줄 수 있는 설치 파일(.apk)을 만드는 방법입니다.

1.  **상단 메뉴 클릭**: `Build` 메뉴를 클릭합니다.
2.  **APK 빌드 선택**: `Build Bundle(s) / APK(s)` > **`Build APK(s)`**를 클릭합니다.
3.  **기다리기**: 우측 하단에 `Gradle Build Running` 알림이 뜹니다.
4.  **파일 찾기**: 완료되면 우측 하단에 `APK(s) generated successfully`라는 알림이 뜹니다. 그 옆의 파란색 글씨 **`locate`**를 클릭하세요.
5.  **완료**: 탐색기가 열리고 `app-debug.apk` 파일이 보입니다. 이 파일을 복사해서 사용하시면 됩니다.

---

### 💡 실행 버튼이 회색(비활성화)인가요?

- 아직 **Gradle Sync**가 안 끝난 상태입니다. 우측 하단의 로딩이 끝날 때까지 조금만 더 기다려 주세요.
- 그래도 안 되면 `File > Sync Project with Gradle Files`를 눌러주세요.
